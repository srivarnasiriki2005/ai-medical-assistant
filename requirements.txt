# ═══════════════════════════════════════════════════════════════
#  AI Medical Assistant — Requirements (All versions verified)
#  Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# Web framework
flask==3.0.3
flask-cors==4.0.1

# HTTP (Groq API calls)
requests==2.32.3

# Environment variables (.env file support)
python-dotenv==1.0.1

# Vector database (VERIFIED version — 1.8.0 does NOT exist)
faiss-cpu>=1.8.0

# Sentence embeddings
sentence-transformers==3.0.1

# NumPy <2.0 required (faiss-cpu incompatible with numpy 2.x)
numpy>=1.24.0,<2.0.0

# Text splitting (no langchain dependency — built-in fallback in app.py)
# Install only if you want the full langchain splitter:
# langchain-text-splitters==0.0.1

# ML & Data Science
scikit-learn==1.5.1
pandas==2.2.2

# PDF support
pypdf==4.3.1
